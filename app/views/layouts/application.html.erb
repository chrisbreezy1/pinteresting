<!DOCTYPE html>
<html>
<head>
  <title>PinterestingOnemonthrails</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <%# required to to format properly on mobile devices%>

</head>
<body>
	
	<%= render 'layouts/header' %>
	<div class="container-fluid">
		<% flash.each do |name, msg| %><%# Flash messages are messages that only pop up once, ie login failed %>
			<%= content_tag(:div, msg, class: "alert alert-info") %>
		<% end %><%# no = sign after percent if we do not want the text to show on site %>
		<%= yield %>
	</div>

</body>
</html>
